Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#states#create()
  let state.log = ['Details on what you won...']
  call state.actions().addNoop('c',  'Continue')

Execute (draw):
  call vimcastle#screens#win#draw(screen, state)

Expect (win screen):
  ---------- You win! ----------
  
  Details on what you won...
  
  c) Continue
