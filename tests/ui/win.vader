Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#state#create()
  let state.log = ['Details on what you won...']
  call state.nav.addNoop('c',  'Continue')

Execute (draw):
  call vimcastle#ui#win#draw(screen, state)

Expect (win screen):
  ---------- You win! ----------
  
  Details on what you won...
  
  c) Continue
