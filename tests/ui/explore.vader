Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#states#create()
  let state.scene = { 'info': { 'label': 'Plains' } }
  let state.player = vimcastle#character#create({'long': 'Player', 'short': 'You'}, 153)
  call state.actions().addNoop('x',  'Action')
  let state.log = ['Stuff happens!']

Execute (draw):
  call vimcastle#screens#explore#draw(screen, state)

Expect (explore screen):
  ----------- Plains -----------
  
                   153 [-------]
  
  Stuff happens!
  
  x) Action
