Execute (test setup):
  let screen = { 'width': 30 }
  let state = {}
  let state.scene = { 'label': 'Plains' }
  let state.player = vimcastle#character#create({'long': 'Player', 'short': 'You'}, 153, {})
  let state.actions = vimcastle#actions#create()
  call state.actions.addNoop('x',  'Action')
  let state.log = ['Stuff happens!']

Execute (draw):
  call vimcastle#ui#explore#draw(screen, state)

Expect (explore screen):
  ----------- Plains -----------
  
                   153 [-------]
  
  Stuff happens!
  
  x) Action
