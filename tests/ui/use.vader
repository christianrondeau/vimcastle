Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#state#create()
  call state.actions().addNoop('b',  'Back')

Execute (draw):
  call vimcastle#ui#use#draw(screen, state)

Expect (use screen):
  ------------- Use ------------
  
  Select the item you want to use.
  
  b) Back

Execute (with log):
  call state.addlog('You feel better!')
  call vimcastle#ui#use#draw(screen, state)

Expect (use screen):
  ------------- Use ------------
  
  You feel better!
  
  Select the item you want to use.
  
  b) Back
