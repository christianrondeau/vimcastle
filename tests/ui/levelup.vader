Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#state#create()
  let state.player = vimcastle#character#create({'long': 'Player', 'short': 'You'}, 100)
  let state.player.level = 6
  let state.log = ['Woot!']
  call state.actions().addNoop('1',  'Increase str 5 -> 6')
  call state.actions().addNoop('2',  'Increase dex 2 -> 3')

Execute (draw):
  call vimcastle#ui#levelup#draw(screen, state)

Expect (levelup screen):
  ---------- Level up! ---------
  
        You gained a level!     
  
           Level 6 -> 7         

  Woot!
  
  1) Increase str 5 -> 6
  2) Increase dex 2 -> 3
