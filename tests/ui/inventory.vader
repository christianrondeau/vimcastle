Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#state#create()
  let weapon = vimcastle#equippablegen#weapon('.', 'Swooord', 5, 127).stat('def', 3, 3).invoke()
  let state.player = vimcastle#character#create({'long': 'Player', 'short': 'You'}, 100).equipweapon(weapon)
  let state.log = ['This should not be shown!']
  call state.nav.addNoop('b',  'Back')

Execute (draw):
  call vimcastle#ui#inventory#draw(screen, state)

Expect (inventory screen):
  ---------- Inventory ---------
  
  * Weapon: <Swooord>
    * def: 3
    * dmg: 5-127
  
  b) Back
