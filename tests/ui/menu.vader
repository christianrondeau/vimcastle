Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#states#create()
  call state.actions().addNoop('x',  'Action')
  let state.log = ['This is a menu!']

Execute (draw):
  call vimcastle#screens#menu#draw(screen, state)

Expect (explore screen):
  ---------- Vimcastle ---------
  
  This is a menu!
  
  x) Action
