Execute (test setup):
  let screen = { 'width': 30 }
  let state = vimcastle#state#create()
  call state.nav.addNoop('x',  'Action')
  let state.log = ['This is a menu!']

Execute (draw):
  call vimcastle#ui#menu#draw(screen, state)

Expect (explore screen):
  ---------- Vimcastle ---------
  
  This is a menu!
  
  x) Action
